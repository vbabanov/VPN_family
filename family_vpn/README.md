# family_vpn

Mobile client for the family VPN service.

## Structure

- `lib/` Flutter application code
- `android/` Android target
- `ios/` iOS target
- `test/` Flutter tests

Desktop and web targets were removed from this repository. The backend-side
agent remains in the top-level `VPN_Agent/` directory.
